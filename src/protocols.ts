export type Car = {
  id: number;
  model: string;
  licensePlate: string;
  year: string;
  color: string;
  createAt: Date;
};

export type NewCar = {
  model: string;
  licensePlate: string;
  year: string;
  color: string;
};
